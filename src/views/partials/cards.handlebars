<div class="row d-flex justify-content-center">
   {{#each products}}
  <div class="col-md-3">
    <div class="card m-3 text-center p-3" >
    {{!-- <div class="row g-0"> --}}
      <div class="col-md-12">
        <img src={{this.thumbnail}} class="img-fluid rounded-start" style="max-width: 5rem"alt={{this.title}}>
      {{!-- </div> --}}
      {{!-- <div class="col-md-12"> --}}
        <div class="card-body">
          <h5 class="card-title">{{this.title}}</h5>
          <p class="card-text">{{this.description}}</p>
          <p class="card-text"><small class="text-muted">Precio: ${{this.price}}</small></p>
                  <a href="/products/{{this.id}}" class="btn btn-primary" style="width: 6rem">Detalles</a>
                  <a href="#" class="btn btn-primary" style="width: 6rem">Comprar</a>


            </button>
        </div>
      {{!-- </div> --}}
    </div>
    </div>
  </div>
    {{/each}}
</div>
